<%- include('partials/header') -%>

<main class="container card-container">
  <div class="row justify-content-center">
    <section class="col-6 mt-5">
      <h1>Sign in with email</h1>
      <% if (locals.messages.errors) { %> <% messages.errors.forEach( el => { %>
      <div class="alert alert-danger"><%= el.msg %></div>
      <% }) %> <% } %> <% if (locals.messages.info) { %> <%
      messages.info.forEach( el => { %>
      <div class="alert alert-info"><%= el.msg %></div>
      <% }) %> <% } %>
      <form action="/login" method="POST">
        <div class="mb-3">
          <label for="exampleInputEmail1" class="form-label"
            >Email address</label
          >
          <input
            type="email"
            class="form-control"
            id="exampleInputEmail1"
            aria-describedby="emailHelp"
            name="email"
          />
        </div>
        <div class="mb-3">
          <label for="exampleInputPassword1" class="form-label">Password</label>
          <input
            type="password"
            class="form-control"
            id="exampleInputPassword1"
            name="password"
          />
        </div>
        <button type="submit" class="btn btn-primary">Sign In</button>
      </form>
    </section>
  </div>
</main>

<div class="list-of-userNames">
  <ul class="list-unstyled">
    <h3>list of users</h3>
    <li>nadi@nadi.com nadi1234 </li>
    <li>jr@jr.com  jonathan1</li>
    <li>b@b.com bentleybentley </li>
    <li>frank@frank.com frankfrank </li>
    <li>test@test.com testtest </li>
    <li>p@p.com popcorn1 </li>
  </ul>
</div>

<%- include('partials/footer') -%>
