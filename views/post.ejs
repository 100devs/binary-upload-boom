<%- include('partials/logged-in-header') -%>
<div class="container card-container">
  <!-- <div class="row justify-content-center mt-5">
    <div class="col-6">
      <h2><%= post.title %></h2>
      <p><%= post.caption %></p>
      <p>By: <%= user.User %></p>
      <img class="img-fluid" src="<%= post.image%>" />
      <div class="row justify-content-between">
        <form
          class="col-1"
          action="/post/likePost/<%= post.id %>?_method=PUT"
          method="POST"
        >
          <button class=" <%if(post.likes.includes(user.id)){ %> btn-liked <%} %>
          btn btn-primary fa fa-heart" type="submit"></button>
        </form>
        <h3 class="col-3">Likes: <%= post.likes.length - 1 %></h3>
        <form
          class="col-1"
          action="/post/bookmarkPost/<%= post.id %>?_method=PUT"
          method="POST"
        >
        <button class=" <%if(post.bookmarks.includes(user.id)){ %> bi bi-bookmark-fill <%} else { %> bi bi-bookmark <% }%>
          btn btn-primary" type="submit"></button>
        </form>
        <%if(post.user == user.id){ %>
        <form
          action="/post/deletePost/<%= post.id %>?_method=DELETE"
          method="POST"
          class="col-3"
        >
          <button class="btn btn-primary fa fa-trash" type="submit"></button>
        </form>
        <%}%>
      </div>
    </div>
    <div class="col-3 mt-5">
      <h3>Ingredients</h3>
      <% for(var i=0; i<post.description.length; i++) {%>
      <p>- <%= post.ingredients[i] %></p>
      <% } %>
      <h3>Reason</h3>
      <ol class="list-group list-group-numbered">
        <% for(var i=0; i<post.reason.length; i++) {%>
          <li class=""><%= post.directions[i] %></li>
        <% } %>
        </ol>
      <!-- <p><%= post.directions %></p> -->
    <!-- </div>
    <div class="col-6 mt-5">
      <a class="btn btn-primary" href="/profile">Return to Profile</a>
      <a class="btn btn-primary" href="/feed">Return to Feed</a>
    </div>
  </div> --> 


  
    <!-- <h1><%= post.title %></h1>
    <div class="row mt-5">
      <div class="col-6">
        <img src="<%= post.image%>" class="img-fluid" alt="Responsive image">
      </div>
    </div>
    <div class="row">
      <div class="col-1">
        <h3>Recipe</h3>
        <h2>Ingredients</h2>
        <ul class="list-group">
        <% for(var i=0; i<post.description.length; i++) {%>
            <li class=""><%= post.ingredients[i] %></li>
            <% } %>
          </ul>
        <h2>Discription</h2>
        <ol class="list-group list-group-numbered">
          <% for(var i=0; i<post.discription.length; i++) {%>
          <li class=""><%= post.directions[i] %></li>
          <% } %>
        </ol>
      </div>
    </div> -->
 
    <div class="row">
      <h1><%= post.title %></h1>
      <p><%= post.caption %></p>
      <p>By: <%= user.userName %></p>
      <div class="col-md-6">
        <img src="<%= post.image%>" class="img-fluid" alt="Responsive image">
        <div class="row row-cols-3">
          <form
            class="col-2"
            action="/post/likePost/<%= post.id %>?_method=PUT"
            method="POST"
          >
            <button class=" <%if(post.likes.includes(user.id)){ %> btn-liked <%} %>
            btn btn-primary fa fa-heart" type="submit"></button>
          </form>
          <h3 class="col"><%= post.likes.length - 1 %></h3>
          <form
            class="col justify-content-end"
            action="/post/bookmarkPost/<%= post.id %>?_method=PUT"
            method="POST"
          >
          <button class="<%if(post.bookmarks.includes(user.id)){ %> bi bi-bookmark-fill<%} else { %> bi bi-bookmark <% }%>
            btn btn-primary" type="submit"></button>
          </form>
          <!-- <%if(post.user == user.id){ %>
          <form
            action="/post/deletePost/<%= post.id %>?_method=DELETE"
            method="POST"
            class="col-3"
          >
            <button class="btn btn-primary fa fa-trash" type="submit"></button>
          </form>
          <%}%> -->
        </div>
      </div>
      <div class="col-md-6">
        <h3 class="display-4">Movie</h3>
        <h2>Description</h2> <hr class="style1">
        <ul class="list-unstyled">
        <% for(var i=0; i<post.description.length; i++) {%>
            <li class="p-1 g-2"><%= post.description[i] %></li>
            <% } %>
          </ul>
          <h2>Reason</h2> <hr class="style1">
          <ol class="list-group list-group-numbered">
            <% for(var i=0; i<post.directions.length; i++) {%>
            <li class="p-1"><%= post.reason[i] %></li>
            <% } %>
          </ol>
      </div>
    </div>

  
  

</div>

<!-- <div class="container">
  <div class="card" style="width: 50rem;">
    <h3 class="card-title"><%= post.title %></h3>
    <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
    <img src="<%= post.image%>" class="card-img-top" alt="...">
    <div class="card-body">
    </div>
    <ul class="list-group list-group-flush">
      <li class="list-group-item">An item</li>
      <li class="list-group-item">A second item</li>
      <li class="list-group-item">A third item</li>
    </ul>
    <div class="card-body">
      <a href="#" class="card-link">Card link</a>
      <a href="#" class="card-link">Another link</a>
    </div>
  </div>
  </div> -->

<%- include('partials/footer') -%>