<% let feedTitle = (typeof(title) !== 'undefined') ? title : site.pages['feed'].title %>
<% page = (typeof(page) !== 'undefined') ? page : site.pages['feed'].page %>

<%- include('layouts/head', { title: feedTitle }) -%>

<%- include('partials/feed') %>

<%- include('layouts/foot') %>
